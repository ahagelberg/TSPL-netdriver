#!/usr/bin/env bash
# Usage: ./start
# Verbose logs: ./start --log debug
set -euo pipefail
cd "$(dirname "$0")"
if [[ -x .venv/bin/python ]]; then
  exec .venv/bin/python -m tspl_driver "$@"
fi
exec python3 -m tspl_driver "$@"
